<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.mappers.SBoardMapper">

	<select id="list" parameterType="com.study.sboard.SBoardVO.SBoardVO" 
	resultType="com.study.sboard.SBoardVO.SBoardVO">
		select sno, title, writer, view_cnt FROM stbl
	</select>
	
	<insert id="write" parameterType="com.study.sboard.SBoardVO.SBoardVO">
		insert into stbl(title, writer, content)
			values(#{title}, #{writer}, #{content})
	</insert>
	
	<select id="read" parameterType="int" 
	resultType="com.study.sboard.SBoardVO.SBoardVO">
		select sno, title, writer, content, view_cnt 
			FROM stbl
			WHERE sno=#{sno}
	</select>
	
	<delete id="delete" parameterType="int">
		delete from stbl where sno=#{sno}
	</delete>
	
	<update id="modify" parameterType="com.study.sboard.SBoardVO.SBoardVO">
		update stbl
			SET title=#{title},
				writer=#{writer},
				content=#{content}
			WHERE sno=#{sno}
	</update>
</mapper>